import React from 'react';
import {View, Text} from 'react-native';

const MyLocations = () => {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
        backgroundColor: 'orange',
      }}>
      <Text>MyLocations</Text>
    </View>
  );
};

export default MyLocations;
